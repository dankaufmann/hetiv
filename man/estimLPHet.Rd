% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimLPHet.R
\name{estimLPHet}
\alias{estimLPHet}
\title{Estimate impulse responses via heteroscedasticity-based IV local projections}
\usage{
estimLPHet(y, O, Ind, Interact, E, P, cumRep, H, HStep, NormFac)
}
\arguments{
\item{y}{Numeric matrix of outcome variables (T x N). The first variable in
each dimension is normalized to unity at horizon 0. These variables are
also used as instruments and for recursive ordering.}

\item{O}{Numeric matrix of information set variables (T x M). May be
identical to \code{y}. Included as lags 1 through \code{P}.}

\item{Ind}{Integer vector of length T, event indicator:
\itemize{
\item \code{0} Control day (no event)
\item \code{1} Policy day (event)
\item \code{2} Contaminated control day (excluded from estimation)
}}

\item{Interact}{Logical. If \code{TRUE}, lagged information set variables are
interacted with event/non-event dummies.}

\item{E}{Integer. Number of shock dimensions to identify via recursive
heteroscedasticity ordering.}

\item{P}{Integer. Maximum lag order for the information set. Set to \code{0} for
no lags (regression on constant only).}

\item{cumRep}{Logical vector of length N. For each variable in \code{y}, whether
to report the cumulative impulse response instead of the level response.}

\item{H}{Integer. Maximum horizon (in periods) up to which IRFs are computed.}

\item{HStep}{Integer. Step size between horizons. If \verb{> 1}, only every
\code{HStep}-th horizon is estimated starting from \code{H = 1}.}

\item{NormFac}{Numeric scalar. Multiplies all IRFs to normalize the impact
response of the first variable to a specific value. Set to \code{1} for no
normalization.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{irf}}{Array (H x N x E) of estimated impulse responses.}
\item{\code{se}}{Array (H x N x E) of HC0 heteroscedasticity-robust standard
errors.}
\item{\code{IVRes}}{List of \code{ivreg} model objects, one per horizon, variable,
and shock dimension.}
\item{\code{Obs}}{Data frame with observation counts: \code{Tp} (policy days),
\code{Tc} (control days), \code{To} (contaminated days), \code{Tt} (total used).}
\item{\code{Method}}{Character string \code{"Heteroscedasticity-IV"}.}
\item{\code{et}}{Data frame of OLS residuals on event days (used for
covariance estimation).}
\item{\code{Sig}}{Covariance matrix of residuals on event days.}
\item{\code{SigR}}{Covariance matrix of residuals on control days, or \code{NA}
if unavailable.}
\item{\code{Psi}}{Impact matrix (N x E), i.e. \code{irf[1, , ]}.}
\item{\code{WeakData}}{Data frame of endogenous variables and instruments for
the Lewis-Mertens (2024) weak instrument test.}
}
}
\description{
Estimates impulse response functions (IRFs) using recursive
heteroscedasticity-IV identification (Rigobon, 2003; Lewis, 2022) combined
with local projections (Jordà, 2005). Identification exploits the difference
in variance between policy event days and control days to construct
instruments for the endogenous variables.
}
\references{
Burri, M. and Kaufmann, D. (2026). Multiple monetary policy shocks from
daily data: A heteroskedasticity IV approach. IRENE Working Paper 26-06,
IRENE Institute of Economic Research. (Multi-dimensional shock identification
via recursive heteroscedasticity ordering, i.e. \code{E > 1}.)

Jordà, Ò. (2005). Estimation and Inference of Impulse Responses by Local
Projections. \emph{American Economic Review}, 95(1), 161–182.

Lewis, D. J. (2022). Robust Inference in Models Identified via
Heteroskedasticity. \emph{Review of Economics and Statistics}, 104(3), 510–524.

Rigobon, R. (2003). Identification Through Heteroskedasticity.
\emph{Review of Economics and Statistics}, 85(4), 777–792.

Rigobon, R. and Sack, B. (2004). The impact of monetary policy on asset
prices. \emph{Journal of Monetary Economics}, 51(8), 1553–1575.
}
