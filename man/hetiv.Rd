% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hetiv.R
\name{hetiv}
\alias{hetiv}
\title{Estimate impulse responses via heteroskedasticity-based IV local projections}
\usage{
hetiv(
  y,
  O,
  X = NULL,
  Ind,
  P,
  H,
  E = 1,
  norm = 1,
  interact = FALSE,
  cum = FALSE,
  Hstep = 1,
  cov_type = "HC0",
  details = FALSE
)
}
\arguments{
\item{y}{Numeric matrix of stationary outcome variables (T x N). The effect on
the first variable in each dimension is normalized to unity at horizon 0.
These variables are also used to construct heteroskedasticity-based instruments
and for recursive ordering to identify multiple dimensions.}

\item{O}{Numeric matrix of information set variables (T x M). May be
identical to \code{y}. Included as lags 1 through \code{P}.}

\item{X}{Numeric matrix of deterministic variables (T x K). For example,
time trend, seasonal dummies or other deterministic controls. Included as is (no lags).
A constant is included by default.}

\item{Ind}{Integer vector of length T, event indicator:
\itemize{
\item \code{0} Control day (no event)
\item \code{1} Policy day (event)
\item \code{2} Contaminated control day (excluded from estimation)
}}

\item{P}{Integer. Maximum lag order for the information set. Set to \code{0} for
no lags (regression on deterministic terms only).}

\item{H}{Integer. Maximum horizon (in periods) up to which IRFs are estimated.}

\item{E}{Integer. Number of shock dimensions to identify via recursive
ordering.}

\item{norm}{Numeric scalar. Normalize the impact response of the first
variable to a specific value. Set to \code{1} for standard unit-effect
normalization.}

\item{interact}{Logical. If \code{TRUE}, lagged information set variables are
interacted with event/non-event dummies.}

\item{cum}{Logical vector of length N. For each variable in \code{y}, whether
to report the cumulative impulse response instead of the level response.
If only one provided, applied to all impulse responses.}

\item{Hstep}{Integer. Step size between horizons. The default \code{1} estimates
all horizons 0 through H - 1. Values greater than 1 estimate only the
selected horizons.}

\item{cov_type}{Covariance estimator for local-projection standard errors:
\code{"HC0"} (default) for heteroskedasticity-robust standard errors or \code{"NW"}
for Newey-West HAC standard errors.}

\item{details}{Logical. If \code{TRUE}, code saves detailed IV results, which is slightly slower.
if set to \code{FALSE}, returns only impulse response and standard error (e.g. for bootstrap)}
}
\value{
A named list with the following elements:
\describe{
\item{\code{irf}}{Array (H x N x E) of estimated impulse responses.}
\item{\code{se}}{Array (H x N x E) of local-projection standard errors.}
\item{\code{IVRes}}{List of \code{ivreg} model objects, one per horizon, variable,
and shock dimension.}
\item{\code{Obs}}{Data frame with observation counts: \code{Tp} (policy days),
\code{Tc} (control days), \code{To} (contaminated days), \code{Tt} (total used).}
\item{\code{Method}}{Character string \code{"Heteroscedasticity-IV"}.}
\item{\code{et}}{Data frame of OLS residuals on event days (used for
covariance estimation and shock extraction).}
\item{\code{Sig}}{Covariance matrix of residuals on event days (used for
shock extraction).}
\item{\code{SigR}}{Covariance matrix of residuals on control days, or \code{NA}
if unavailable (used for shock extraction).}
\item{\code{Psi}}{Impact matrix (N x E), equal to \code{irf[1, , ]}. By the
package's indexing convention \code{HSeries} starts at 1, so the first LP
uses \code{lead(y, 0)} (the contemporaneous value) and is labelled horizon
0; \code{irf[1, , ]} is therefore always the impact response.}
\item{\code{WeakData}}{Data frame of endogenous variables and instruments for
the Lewis-Mertens (2025) weak instrument test.}
}
}
\description{
Estimates impulse response functions (IRFs) using recursive
heteroskedasticity-IV identification (Rigobon, 2003; Rigobon and Sack, 2004;
Lewis, 2022; Burri and Kaufmann, 2026a, 2026b) combined with local projections
(Jorda, 2005). Identification exploits the difference in variance between
policy event days and control days to construct instruments for the
endogenous variables.
}
\details{
For \code{E > 1}, identification is recursive and order-dependent: the
column order of \code{y} defines both the shock ordering and the normalization
variable for each shock dimension.
}
\examples{
set.seed(1)
y <- matrix(rnorm(80), ncol = 2)
Ind <- rep(0L, nrow(y))
Ind[seq(5, nrow(y), by = 5)] <- 1L
res <- hetiv(y = y, O = y, Ind = Ind, P = 1, H = 3, details = TRUE)
dim(res$irf)

}
\references{
Burri, M. and D. Kaufmann (2026a). Measuring monetary policy shocks.
IRENE Working Papers 24-03, IRENE Institute of Economic Research,
University of Neuchatel.

Burri, M. and D. Kaufmann (2026b). Multiple monetary policy shocks from
daily data: A heteroskedasticity IV approach. IRENE Working Papers 26-06,
IRENE Institute of Economic Research, University of Neuchatel.

Jorda, O. (2005). Estimation and inference of impulse responses by local
projections. \emph{American Economic Review}, 95(1), 161-182.

Lewis, D. J. (2022). Robust inference in models identified via
heteroskedasticity. \emph{Review of Economics and Statistics}, 104(3), 510-524.

Lewis, D. J. and Mertens, K. (2025). A robust test for weak instruments for 2SLS with multiple
endogenous regressors. \emph{The Review of Economic Studies}, DOI: 10.1093/restud/rdaf103

Rigobon, R. (2003). Identification through heteroskedasticity.
\emph{Review of Economics and Statistics}, 85(4), 777-792.

Rigobon, R. and Sack, B. (2004). The impact of monetary policy on asset
prices. \emph{Journal of Monetary Economics}, 51(8), 1553-1575.
}
