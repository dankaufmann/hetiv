% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotpval.R
\name{plotpval}
\alias{plotpval}
\title{Plot p-values across horizons}
\usage{
plotpval(pvals, HTick, Labels, sigLevels = c(0.05, 0.1))
}
\arguments{
\item{pvals}{Array (H x N x E) of p-values. Dimnames on the first dimension
must be the horizon labels (numeric).}

\item{HTick}{Integer. Step size for x-axis tick marks.}

\item{Labels}{Character vector of length N. Variable names used as panel titles.}

\item{sigLevels}{Numeric vector of significance levels at which to draw
horizontal reference lines. The first value is drawn as a solid line, the
second as dashed, and any further values as dotted. Defaults to
\code{c(0.05, 0.10)}.}
}
\value{
A list of \code{ggplot} objects, one per variable-shock combination,
ordered by shock (outer loop) then variable (inner loop).
}
\description{
Produces a panel of p-value plots across IRF horizons, with optional
horizontal reference lines at user-supplied significance levels. Returns a
list of \code{ggplot} objects (one per variable-shock combination) that can be
arranged with e.g. \code{cowplot::plot_grid()}.
}
\examples{
pvals <- array(c(0.2, 0.08, 0.04), dim = c(3, 1, 1))
dimnames(pvals)[[1]] <- 0:2
plotpval(pvals, HTick = 1, Labels = "Output")

}
