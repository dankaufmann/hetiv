% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulatedata.R
\name{simulatedata}
\alias{simulatedata}
\title{Simulate VAR data with heteroskedastic shocks and given parameters}
\usage{
simulatedata(Phi, SigE, PsiE, PsiR, Nobs, Nbin, N, R, E, Nevn, P, eDist, seed)
}
\arguments{
\item{Phi}{Array of VAR coefficient matrices, dimension \verb{N x N x P}.}

\item{SigE}{Variance of the event shocks. This applies to all E shocks.}

\item{PsiE}{Impact matrix for event shocks, dimension \verb{N x E}.}

\item{PsiR}{Impact matrix for regular shocks, dimension \verb{N x R}.}

\item{Nobs}{Number of observations to retain after burn-in. The returned
data have exactly \code{Nobs} rows.}

\item{Nbin}{Number of burn-in observations. These are simulated to
initialise the VAR but are discarded before returning.}

\item{N}{Number of variables in the VAR.}

\item{R}{Number of regular shocks.}

\item{E}{Number of event shocks. For GARCH shock distributions (\code{eDist = c(alpha, beta)}), only \code{E = 1} is supported.}

\item{Nevn}{Event frequency: an event shock occurs every \code{Nevn} periods.
Set to \code{0} to suppress event shocks entirely (no heteroskedasticity).}

\item{P}{VAR lag order.}

\item{eDist}{Shock distribution. Use \code{0} for standard normal; a positive
integer for Student-t with that many degrees of freedom; or a numeric
vector \code{c(alpha, beta)} for GARCH(1,1) with ARCH effect \code{alpha} and
persistence \code{beta}.}

\item{seed}{Integer seed passed to \code{\link[=set.seed]{set.seed()}} for reproducibility.
Use \code{NA} to skip seeding.}
}
\value{
A named list with components:
\describe{
\item{y}{Simulated VAR data, dimension \verb{Nobs x N} (burn-in discarded).}
\item{IndE}{Event indicator matrix, dimension \verb{Nobs x 1}.}
\item{eR}{Simulated regular shocks, dimension \verb{Nobs x R}.}
\item{eE}{Simulated event shocks, dimension \verb{Nobs x E}.}
\item{e}{Composite structural shocks, dimension \verb{Nobs x N}.}
\item{Phi}{VAR coefficient array (returned unchanged).}
\item{PsiE}{Event shock impact matrix (returned unchanged).}
\item{PsiR}{Regular shock impact matrix (returned unchanged).}
\item{SigE}{Event shock variance (returned unchanged).}
}
}
\description{
Simulates data from a VAR(P) model with two types of structural shocks:
regular shocks (always present) and event shocks (occurring every \code{Nevn}
periods). The shock distribution can be normal, Student-t, or GARCH(1,1).
}
\examples{
N <- 2
Phi <- array(0, dim = c(N, N, 1))
Phi[, , 1] <- diag(c(0.4, 0.2))
simulatedata(
  Phi = Phi, SigE = 2, PsiE = matrix(c(1, 0.5), N, 1),
  PsiR = diag(N), Nobs = 20, Nbin = 5, N = N, R = N, E = 1,
  Nevn = 5, P = 1, eDist = 0, seed = 1
)

}
